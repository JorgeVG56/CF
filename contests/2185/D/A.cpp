/*
  Pura gente del Coach Moy  
*/
#include<bits/stdc++.h>
using namespace std;
using ll = long long;

signed main(){
  cin.tie(0)->sync_with_stdio(0);

  int t; cin >> t;

  for(int _t = 0; _t < t; _t++){
    int n, m, h; cin >> n >> m >> h;
    vector<int> a(n); for(int & x : a) cin >> x;
    vector<pair<int, int>> b(m); for(auto & [x, y] : b) cout >> x >> y;
    
    
  }

  return 0;
}