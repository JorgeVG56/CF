/*
  Pura gente del Coach Moy

  
*/
#include<bits/stdc++.h>
using namespace std;
using ll = long long;

struct FT {
  vector<ll> s;

  FT(int n) : s(n) {}

  void update(int pos, )
}

signed main(){
  cin.tie(0)->sync_with_stdio(0);

  int n, q; cin >> n >> q;

  

  return 0;
}